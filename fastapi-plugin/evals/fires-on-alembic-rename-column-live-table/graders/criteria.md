---
type: llm
---
PASS if the reply lays out a multi-step, multi-deploy expand-contract sequence for the rename: (1) add the new display_name column while full_name stays, (2) dual-write both columns from the application code, (3) backfill display_name from full_name for existing rows, (4) switch application reads over to display_name and deploy, (5) drop the old full_name column only in a later deploy once no running code references it. The reply must also explicitly say that a single revision using op.alter_column(..., new_column_name=...) to rename directly is unsafe against live traffic during a rolling deploy.
FAIL if the reply proposes a single op.alter_column rename (or equivalent one-revision rename) as sufficient, if it only gives generic Alembic hygiene advice (e.g. "write a downgrade()", "test in staging", "check the autogenerated diff") without the specific add/dual-write/backfill/switch/drop sequence, or if it omits the requirement that dropping the old column must wait for a later deploy.
