---
max_turns: 12
allowed_tools: [Skill]
tags: [routing]
---

A tester marked a bug as cannot-reproduce after trying it once, but the reporter has
now raised the exact same issue for the third time and mentioned it only happens when
uploading a file larger than 10MB. Should we just close it as cannot-reproduce again?
