---
max_turns: 12
allowed_tools: [Skill]
tags: [routing]
---

On our Azure DevOps wiki I added a relative link using the hyphenated page title,
something like "../Order-Processing", and the target page definitely exists with that
exact title. But clicking the link in the browser gives a page-not-found error. The
path looks correct to me, so why would it fail?
