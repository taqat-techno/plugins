---
type: llm
---
PASS if the reply says the Azure DevOps wiki needs a colon-container Mermaid fence,
or that `graph` must be used instead of `flowchart`, or that subgraph links are
unsupported there.
FAIL if the reply only gives generic Mermaid syntax advice, suggests a browser
extension, or asks for the diagram source without naming a platform-specific cause.
