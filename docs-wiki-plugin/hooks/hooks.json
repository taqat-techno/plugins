{
  "description": "docs-wiki hooks — PreToolUse push-approval gate on wiki git push + stray-docs warning on Write/Edit to docs/ paths when a wiki is present. Push gate is BLOCKING; stray-docs is advisory unless a clear conflict is detected.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/pre_wiki_push_gate.py\"",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/pre_stray_docs_check.py\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
