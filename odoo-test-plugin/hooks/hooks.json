{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "path": "**/tests/test_*.py",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Test file modified.\n- **New file?** Ensure `tests/__init__.py` imports it — without this, Odoo's test runner will NOT discover the test.\n- **Run coverage**: `/odoo-test coverage /path/to/module --threshold 80`\n- **SavepointCase**: Removed in Odoo 16+. Use `TransactionCase` instead."
          }
        ]
      }
    ]
  }
}
