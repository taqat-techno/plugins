---
max_turns: 8
allowed_tools: [Skill]
tags: [routing]
---

My app has a dark mode toggle switch, but whenever the user refreshes the page it forgets their choice and resets to light mode. How do I persist the toggle state in React so it survives a page reload?
