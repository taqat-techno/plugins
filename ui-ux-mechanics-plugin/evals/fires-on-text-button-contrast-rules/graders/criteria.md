---
type: llm
---
PASS if the reply gives the WCAG 2.1 AA split: normal text (smaller than about 18px) needs at least 4.5:1 contrast, while large text (roughly 18px bold or 24px and up) and UI components/graphical objects such as buttons need at least 3:1, i.e. it explicitly distinguishes the button/large-text threshold from the body-text threshold rather than giving one number for everything.
FAIL if the reply gives only a single blanket contrast number for all text and UI elements, or vague advice like "make sure there is enough contrast" without stating the distinct 4.5:1 versus 3:1 thresholds.
