---
max_turns: 12
allowed_tools: [Skill]
tags: [routing]
---

I need to add a custom scanning screen for warehouse staff in Odoo 19. They are already logged into the backend normally, and I just want the screen to feel like a real dedicated app without the standard navbar and breadcrumbs cluttering the UI. Should I build this as a full standalone OWL application mounted on its own route, with its own router and error handling?
