---
type: llm
---
PASS if the reply says a fully self-rooted OWL app is not needed here, names that only four requirements ever force that shape (a URL outside /odoo, public/unauthenticated users, offline operation, or a different security surface), states that removing the navbar and breadcrumbs alone is not one of them, and instead recommends a client action (ir.actions.client) with target=fullscreen to get the same chrome-free feel while keeping the router and other framework services.
FAIL if the reply simply agrees to build a standalone or self-rooted OWL app without naming the forcing requirements, or gives generic single-page-app architecture advice (pick a router library, structure your components, etc.) without identifying the Odoo-specific ir.actions.client / target=fullscreen alternative.
